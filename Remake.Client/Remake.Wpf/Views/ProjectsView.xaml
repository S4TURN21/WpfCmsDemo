<UserControl x:Class="Remake.Wpf.Views.ProjectsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Remake.Wpf.Views"
             xmlns:viewmodels="clr-namespace:Remake.Wpf.ViewModels"
             xmlns:models="clr-namespace:Remake.Wpf.Models"
             xmlns:p="clr-namespace:Prime.Wpf;assembly=Prime.Wpf"
             mc:Ignorable="d"
             d:FontFamily="Inter"
             d:FontSize="14"
             d:DesignHeight="450"
             d:DesignWidth="800"
             d:Background="#FFFFFF"
             d:DataContext="{d:DesignInstance Type={x:Type viewmodels:ProjectsViewModel}}">
    <Grid>
        <Border Background="#FFFFFF"
                BorderBrush="#E9ECEF"
                BorderThickness="1,1,1,1"
                CornerRadius="12"
                SnapsToDevicePixels="True"
                Padding="28">
            <Grid>
                <DataGrid AutoGenerateColumns="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          Style="{StaticResource DataTable}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Организация"
                                            Binding="{Binding Company}" />
                        <DataGridTextColumn Header="Наименование"
                                            Binding="{Binding Name}" />
                        <DataGridTextColumn Header="Ответственный"
                                            Binding="{Binding User}" />
                        <DataGridTextColumn Header="Дата завершения"
                                            Binding="{Binding DateEnd}" />
                    </DataGrid.Columns>

                    <models:Project Company="ООО &quot;Cтиль&quot;"
                                    Name="СОП047 Стиль СПб - Краснодар"
                                    User="Ермаков К.А."
                                    DateEnd="27.05.25" />
                    <models:Project Company="ООО &quot;СтройКом&quot;"
                                    Name="СОП042 СтройКом СПб - Братское"
                                    User="Ермаков А.А."
                                    DateEnd="30.05.25" />
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
